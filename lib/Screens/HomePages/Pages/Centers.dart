import 'package:flutter/material.dart';

class Usercenters extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'centers page',
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}
import 'package:flutter/material.dart';

class Usercalender extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'calendar page',
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}